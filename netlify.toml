[build]
  publish = "public/"
  command = "yarn install --production=false && yarn documentation:build"

[build.environment]
  NODE_ENV = "production"

[[redirects]]
  from = "/components/gallery"
  to = "/components"
