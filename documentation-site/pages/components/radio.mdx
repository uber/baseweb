<!--
Copyright (c) 2018 Uber Technologies, Inc.

This source code is licensed under the MIT license found in the
LICENSE file in the root directory of this source tree.
-->

import Example from '../../components/example';
import API from '../../components/api';
import Layout from '../../components/layout';

import Basic from 'examples/radio/basic.js';
import Stateful from 'examples/radio/stateful.js';
import HorizontalAlign from 'examples/radio/horizontal-align.js';
import Error from 'examples/radio/error.js';
import Customization from 'examples/radio/overrides.js';
import Disabled from 'examples/radio/disabled.js';

import Overrides from '../../components/overrides';
import {Radio, StatefulRadioGroup} from 'baseui/radio';
import * as RadioExports from 'baseui/radio';

export default Layout;

# Radio

Radios are used when only one choice may be selected in a series of options.

## When to use

- When the user should see all available options.

<Example title="Radio Basic Usage" path="examples/radio/basic.js">
  <Basic />
</Example>

<Example title="Radio as Stateful RadioGroup" path="examples/radio/stateful.js">
  <Stateful />
</Example>

<Example title="Disabled Radios" path="examples/radio/disabled.js">
  <Disabled />
</Example>

<Example
  title="Radio with Horizontal Align"
  path="examples/radio/horizontal-align.js"
>
  <HorizontalAlign />
</Example>

<Example title="Radio in Error State" path="examples/radio/error.js">
  <Error />
</Example>

<Example title="Radio with Overrides" path="examples/radio/overrides.js">
  <Customization />
</Example>

## Overrides

<Overrides
  name="Radio"
  component={RadioExports}
  whitelisted={[
    'Root',
    'RadioMarkInner',
    'RadioMarkOuter',
    'Label',
    'Input',
    'RadioGroupRoot',
  ]}
  renderExample={props => (
    <StatefulRadioGroup
      overrides={{RadioGroupRoot: props.overrides.RadioGroupRoot}}
      name="choose one option"
      initialState={{value: '2'}}
    >
      <Radio overrides={props.overrides} value="1">
        First
      </Radio>
      <Radio overrides={props.overrides} value="2">
        Second
      </Radio>
      <Radio overrides={props.overrides} value="3">
        Third
      </Radio>
    </StatefulRadioGroup>
  )}
/>

<API
  heading="Radio API"
  api={require('!!@uber-web-ui/extract-react-types-loader!../../../src/radio/radio.js')}
/>

<API
  heading="RadioGroup API"
  api={require('!!@uber-web-ui/extract-react-types-loader!../../../src/radio/radiogroup.js')}
/>

<API
  heading="StatefulRadioGroup API"
  api={require('!!@uber-web-ui/extract-react-types-loader!../../../src/radio/stateful-radiogroup.js')}
/>

<API
  heading="StatefulRadioGroupContainer API"
  api={require('!!@uber-web-ui/extract-react-types-loader!../../../src/radio/stateful-radiogroup-container.js')}
/>
