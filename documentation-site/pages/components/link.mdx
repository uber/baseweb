import Yard from "../../components/yard/index";
import linkConfig from "../../components/yard/config/link";
import Layout from "../../components/layout";
import Exports from "../../components/exports";

import * as LinkExports from "baseui/link";

export default Layout;

# Link

<Yard placeholderHeight={20} {...linkConfig} />

<Exports
  component={LinkExports}
  title="Link exports"
  path="baseui/link"
  cheatsheet={false}
/>

Your application may need to draw more attention to some links on the page.
Apply the `animateUnderline` prop to the component if the link is a call-to-action or initiates a UX flow.
