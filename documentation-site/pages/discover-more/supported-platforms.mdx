import Layout from "../../components/layout";
import { Table } from "baseui/table";

export default Layout;

# Supported Platforms

Base Web supports the latest, stable releases of all major browsers and platforms.

<Table
  columns={["Browser", "Supported version"]}
  data={[
    ["Chrome", ">= 58"],
    ["Firefox", "ESR"],
    ["Safari", ">= 10.1"],
    ["Edge", ">= 16"],
    ["iOS Safari", ">= 10.3"],
    ["Android", ">= 4.4"],
  ]}
/>

**If you decide to opt-in to use the [overrides](https://baseweb.design/guides/understanding-overrides/) property, you are responsible to make sure it works in all browsers, as we cannot guarantee that.**
